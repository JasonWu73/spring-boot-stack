
web-kit:
  security:
    api-path-prefix: /test-api/
    permit-all-paths:
      - /test-api/v1/auth/login
      - /test-api/v1/public/**
  spa:
    file-path: classpath:/static/test-index.html
spring:
  main:
    banner-mode: off